# 大数据处理综合实验课程 (2026)

课程设计-1：电商用户行为特征挖掘与预测

南京大学 计算机学院

---

h=edloop

课程设计：天猫商城复购预测

## 1.课程设计目标

本课程设计以真实的电商用户行为日志为基础，面向“推荐系统与用户画像”业务场景，要求同学使用 Spark RDD 完成底层数据处理、特征提取、用户聚类与个性化推荐的完整数据挖掘链路。

## 2.学习技能

通过本课程设计，学生将学习和掌握以下技能：

1. 掌握基于海量时序日志提取结构化特征的方法与归一化技巧。
2. 熟练使用 RDD 核心算子（如 `aggregateByKey`、`join`、`groupByKey` 等）解决复杂的聚合与倒排问题。
3. 理解并实现 K-Means 聚类与随机游走等分布式算法。
4. 避免对高级封装库（如 MLlib、GraphX）的依赖，深入理解分布式算法的底层 MapReduce 迭代逻辑。

---

h=edloop

^{}[]

# 课程设计：天猫商城复购预测

- 3.数据集介绍
- 3.1 背景

使用阿里云天池公开的淘宝/天猫用户行为数据集 (User Behavior Data)，经过精简后的数据集。该数据集记录了用户在一段时间内的各项交互行为。

---

In-adoo

简

# 课程设计：天猫商城复购预测

## 3. 数据集介绍

### 3.2 字段说明

每条原始日志表示一次用户行为，核心字段如下：

* `user_id`：用户唯一标识。
* `item_id`：商品唯一标识（用于构建倒排索引）。
* `category_id`：商品所属类目。
* `behavior_type`：行为类型，包含 4 种（`pv` 浏览、`cart` 加购、`fav` 收藏、`buy` 购买）。
* `timestamp`：行为发生的时间戳（Unix 时间戳，精确到秒）。

---

In-athoop

#

# 课程设计：天猫商城复购预测

- 3. 题目描述
- 3.3 文件结构描述

数据集位置：/user/root/final_exp/exp1/

![img-0.jpeg](assets/课程设计_img-0.jpeg)

![img-1.jpeg](assets/课程设计_img-1.jpeg)

---

h=tdloop

#

# 课程设计：天猫商城复购预测

## 4.实验任务

- 任务一：数据清洗与用户统计特征提取

目标：清洗脏数据，并利用 RDD 分组聚合算子，为每个用户构建量化的 3 维特征向量。

具体要求：

1. 数据清洗：过滤缺失值记录及 `behavior_type` 不合法的记录。
2. 特征聚合：以 `user_id` 为 Key，提取以下 3 个维度的特征：

* 时效特征 $F_1$ ：用户最后一次发生行为的时间戳距离整体截止时间的差值（天数）。整体截止时间定义为全量数据集中的最大时间戳。
* 活跃度特征 $F_2$ ：用户的总交互次数。
* 价值特征 $F_3$ ：依据行为赋分（`buy`=4, `cart`=3, `fav`=2, `pv`=1），计算总得分。

3. 特征归一化：自行使用 RDD 算子求出全局极值，实现 Min-Max 归一化，将三维特征缩放至 [0, 1] 区间。

---

In-2doop

华

# 课程设计：天猫商城复购预测

## 4.实验任务

- 任务一：数据清洗与用户统计特征提取

## 输出要求：

- 文件 1: `clean_behavior_log.csv`
- 格式: `user_id, item_id, category_id, behavior_type, timestamp`
- 样例: `1001,88231,102,buy,1511544070`
- 文件 2: `user_features.csv`
- 格式: `user_id, F1_norm, F2_norm, F3_norm`（保留 4 位小数）
- 样例: `1001,0.9211,0.1532,0.4420`

---

h=tdloop

^{}[]

# 课程设计：天猫商城复购预测

## 4.实验任务

- 任务二：基于行为标签的倒排索引与共现分析
- 目标：构建商品到用户的倒排索引，并利用倒排计算商品间的共现频次。

具体要求：

1. 过滤出产生过深度交互（`cart`, `fav`, `buy`）的日志记录。
2. 以 `item_id` 为键，构建商品到用户的倒排索引。
3. 利用提取出的 `(user_id, item_id)` 关系对，计算两两商品被同一个用户交互的共现次数（即计算 Jaccard 相似度的分子部分）。

---

In-3doop

课程设计：天猫商城复购预测

## 4.实验任务

- 任务二：基于行为标签的倒排索引与共现分析

## 输出要求：

- 文件 1：`item_user_inverted_index.txt`
- 格式：`item_id \t user_id_1, user_id_2, user_id_3...`
- 样例：`88231 \t 1001, 1005, 2034, 5099`
- 文件 2：`item_co_occurrence.txt`
- 格式：`item_id_A, item_id_B \t co_occurrence_count`（按共现次数降序排列前 1000 条）
- 样例：`88231, 44512 \t 156`

---

h=3doop

简

# 课程设计：天猫商城复购预测

## 4.实验任务

- 任务三：K-Means 用户聚类
- 目标：基于任务一提取的归一化特征，手工实现 K-Means 算法，对用户进行价值分群。

具体要求：

1. 随机抽样初始化 K 个聚类中心（建议 K=3 或 5）。
2. 严禁调用 MLlib。必须通过 RDD 算子实现迭代：计算距离 → 分配类簇 → 聚合更新中心点。
- 使用欧氏距离作为相似度度量标准。
3. 使用广播变量（Broadcast）下发聚类中心以优化性能。
4. 设定合理的迭代终止条件（最大迭代次数或中心点偏移量极小值）。

---

h=edloop

#

# 课程设计：天猫商城复购预测

## 4.实验任务

- 任务三：实现 K-Means 用户聚类

## 输出要求：

- 文件 1：`cluster_centers.txt`
- 格式：`cluster_id \f1_center, F2_center, F3_center`
- 样例：`0 \f0.8521,0.6632,0.7011`
- 文件 2：`user_cluster_labels.csv`
- 格式：`user_id, cluster_id1`
- 样例：`1001,0`

---

Theedoop

华

# 课程设计：天猫商城复购预测

## 4. 实验任务

- 任务四：基于二分图随机游走的 PersonalRank 推荐
- 目标：将传统的 PageRank 改造为 User-Item 二分图上的 PersonalRank 算法，实现个性化推荐。

具体要求：

1. 构建二分图：利用任务 2 中的交互关系，构建 User 与 Item 构成的无向二分图。
2. 算法实现：设定 1 个特定的目标用户，手工实现带重启（阻尼系数 $\alpha=0.85$）的随机游走迭代。
3. 严禁调用 GraphX。必须通过 RDD 的 `join` 与 `reduceByKey` 算子实现状态矩阵的迭代更新。
4. 输出目标用户的 Top 10 个性化推荐商品列表（需过滤掉用户已交互过的商品）。

---

In-adoop

课程设计：天猫商城复购预测

## 4.实验任务

- 任务四：基于二分图随机游走的 PersonalRank 推荐

## 输出要求：

- 文件：`personal_rank_recommendations.txt`
- 格式：`target_user_id \trec_item_1:score,rec_item_2:score,...`
- 样例：`1001 \t 44512:0.045,88231:0.038,11234:0.031`

---

hadoop

课程设计：天猫商城复购预测

## 5.课程设计要求

### 1.提交材料

1. 程序源代码，要求提供包含完整目录结构的代码包，并提供编译和执行方法说明
2. 程序可执行jar包以及jar包的执行方式，本课程设计的运行环境为hadoop-3.2.1、jdk1.8
3. 课程设计报告pdf，报告内容包括每个子任务设计的程序设计的主要流程，程序采用的主要算法，进行的优化工作，优化所取得的效果、程序的性能分析以及程序的运行截图等。

课程设计报告中描述小组信息（组员姓名、学号、联系方式）和小组分工情况：明确各成员在课程设计中分工的内容，要求以 git 形式分工合作（小组组长在 git.nju.edu.cn 创建一个项目仓库，其余小组成员以开发者身份加入该项目，注意要在仓库中备注小组信息）。

---

In-3doop

课程设计：天猫商城复购预测

## 5.课程设计要求

### 2.完成周期

课程设计完成周期为一个月，验收日期为本学期**最后一次实验课**

后续实验课要求到教室完成课程实验

提交方式：上传至课程网站

### 3.提醒

1. 设计程序时可以有自己的解决方案，不一定要按照给定提示进行，鼓励探索新的方法
2. 提交材料时请务必检查好是否遗漏，文件格式是否符合要求
3. 请勿抄袭，一旦发现抄袭必严惩
4. 先使用提供的部分数据集在本地进行验证，确认无误后再使用大数据平台运行完整数据